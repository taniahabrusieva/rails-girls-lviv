<p id="notice"><%= notice %></p>

<h1 class='text-center'>My awesom Ideas</h1>

<table class='table table-striped table-bordered text-center'>
  <thead>
    <tr>
      <th>Name</th>
      <th>Description</th>
      <th>Picture</th>
      <th colspan="3">Action</th>
    </tr>
  </thead>

  <tbody>
    <% @ideas.each do |idea| %>
      <tr>
        <td><%= idea.name %></td>
        <td><%= idea.description %></td>
        <td><%= image_tag idea.picture.url(:thumb) %></td>
        <td><%= link_to 'Show', idea, class: 'btn btn-warning' %></td>
        <td><%= link_to 'Edit', edit_idea_path(idea), class: 'btn btn-info' %></td>
        <td><%= link_to 'Destroy', idea, method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-danger' %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Idea', new_idea_path, class: 'btn btn-info' %>
